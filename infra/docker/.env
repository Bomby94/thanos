# infra/docker/.env

# 🧠 Environment settings
APP_ENV=dev
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=app_db
MYSQL_USER=app
MYSQL_PASSWORD=password

# 🧑 UID/GID mapping for dev containers
UID=1000
GID=1000

# Traefik / app-specific configs
TRAEFIK_DASHBOARD_DOMAIN=traefik.thanos.local
TRAEFIK_PHP_DOMAIN=infinity.thanos.local
